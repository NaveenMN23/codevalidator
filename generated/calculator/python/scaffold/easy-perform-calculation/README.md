# Calculator: Perform a Calculation

## Problem Statement
The performCalculation() function body is missing. Implement it to execute basic arithmetic operations (add, subtract, multiply, divide) based on the input operation type. Handle division by zero and invalid operations.

## Instructions
1. Explore the codebase to understand the existing structure and patterns.
2. Implement the target function — look for the `// TODO` or `# TODO` comment.
3. Handle all edge cases (not-found, forbidden, invalid state).
4. Run the tests to verify your implementation.

## How to Run
1. `pip install -r requirements.txt`
2. `uvicorn main:app --reload`
3. `pytest`
